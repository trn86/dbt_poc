version: 2

models:
  - name: stg_customers
    description: customers model tests
    columns:
      - name: customer_id
        description: customer_id is the unique key of the table
        tests:
          - unique
          - not_null

  - name: stg_orders
    description: orders model tests
    columns:
      - name: order_id
        description: order_id is the unique key of the table
        tests:
          - unique
          - not_null